<p>Salut,</p>

<span><%= @user.first_name %> <%= @user.last_name %> t'invite à rejoindre easy-coloc</span>

<br><br><span>Afin d'organiser au mieux votre collocation, <%= @user.first_name %>
  a créé :
</span>

<h3><%= @flat.name %></h3>

<br><br><p>Pour pouvoir rejoindre la collocation et aider <%= @user.first_name %>, clcique sur le bouton ci-dessous</p>

<a href="<%= [ApplicationGlobals::GlobalVar::BASE_URL, ApplicationGlobals::GlobalVar::SIGN_UP_PATH].join %>" class="btn-primary" itemprop="url">Rejoindre <%= @user.first_name %></a>
