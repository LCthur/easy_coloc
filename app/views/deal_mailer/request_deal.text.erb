Salut <%= @target_user["first_name"] %> <%= @target_user["last_name"] %>,

<%= @user.first_name %> <%= @user.last_name %> veut échanger cette tâche avec vous:

<%= @initial_task["name"] %>
Description: <%= @initial_task["description"] %>

Pour la raison suivante:

<%= @deal.description %>

Si vous êtes d'accord avec de cet échange, veuillez cliquer sur le bouton ci-dessous.

Accepter l'échange!
<%= [ApplicationGlobals::GlobalVar::BASE_URL, ApplicationGlobals::GlobalVar::DEALS_PATH].join %>
